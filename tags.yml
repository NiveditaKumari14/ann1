--- 
-
 host:all
 tasks: 
   - 
    loop: 
      - httpd
      - memcached
    tags: 
      - packages
    yum: 
      name: "{{ item }}"
      state: installed
   - 
    tags: 
      - configuration
    template: 
      dest: /etc/foo.conf
      src: templates/src.j2
